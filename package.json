{
  "name": "rendering-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run server.js",
    "check": "bun run hyperframes lint && bun run hyperframes validate && bun run hyperframes inspect",
    "render": "bun run hyperframes render",
    "publish": "bun run hyperframes publish"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.28.0",
    "hyperframes": "0.6.69"
  }
}
